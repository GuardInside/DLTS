#ifndef INTERPOLATION_H_INCLUDED
#define INTERPOLATION_H_INCLUDED
#include <gsl/gsl_interp.h>
#include <vector>
using namespace std;

#define MIN_SIZE_INTERP     3

class interp
{
    private:
        gsl_interp_accel *acc;  //Ускоритель доступа
        gsl_interp *inter_poly; //Указатель на интерполянт
        size_t size;            //Размер сетки
        double *x;              //Указатели на миссивы сетки
        double *y;
    public:
        interp(const vector<double> &xAxis, const vector<double> &yAxis, size_t s, const gsl_interp_type *type): size(s){}
        double at(double x_0);
        ~interp();
};


#endif // INTERPOLATION_H_INCLUDED
